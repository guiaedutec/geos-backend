= simple_form_for @region, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = error_span(@region[:name])
  = f.button :submit, :class => 'btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), regions_path, :class => 'btn btn-default'
